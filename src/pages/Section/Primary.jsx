

function Alpha(props) {
	return (
		<main>
			<h1>Primary Component</h1>
			<div>
				<p>Simple route in a shared path (<i className="code">Section</i>) and its own component.</p>
				<p className="code">/pages/Section/Primary.jsx</p>
				<p>This is called in using the simple component in route. (see About example).
					</p>
			</div>
		</main>
	);
}

export default Alpha;